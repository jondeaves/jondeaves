export default interface ITheme {
  color: {
    fanduelWhite: string;

    fanduelBlue: string; // Link
    fanduelBlueD2: string; // Link hover

    fanduelGrey: string; // Border
    fanduelGreyL4: string; // Screen background
    fanduelGreyD1: string; // Disabled
    fanduelGreyD4: string; // Copy

    fanduelNavy: string; // Headings
  };
  spacing: {
    tiny: string;
    xsmall: string;
    small: string;
    medium: string;
    large: string;
  };
  typography: {
    lineheight: {
      theta: string;
      eta: string;
      zeta: string;
      epsilon: string;
      delta: string;
      gamma: string;
      alpha: string;
      giga: string;
      mega: string;
    };
    sizes: {
      theta: string;
      eta: string;
      zeta: string;
      epsilon: string;
      delta: string;
      gamma: string;
      alpha: string;
      giga: string;
      mega: string;
    };
  };
}
